{
  "name": "alpaca-trade-api-tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Reproducible OpenAPI regeneration pipeline for @alpacahq/alpaca-trade-api (Trading + Market Data).",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate": "tsx src/cli.ts",
    "jar:fetch": "bash scripts/ensure-jar.sh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.40.1",
    "@types/node": "^24.0.0",
    "fast-json-patch": "^3.1.1",
    "tsx": "^4.22.4",
    "typescript": "^5.4.0",
    "vitest": "^4.1.11"
  }
}
