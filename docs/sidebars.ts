import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

/**
 * Single auto-generated sidebar: it picks up the curated guide pages plus the
 * generated `api/` reference section (emitted into the docs tree at build time
 * by `scripts/gen-docs-api-reference.ts`), so new guides and regenerated API
 * pages appear without hand-editing this file.
 */
const sidebars: SidebarsConfig = {
    docsSidebar: [{ type: "autogenerated", dirName: "." }],
};

export default sidebars;
