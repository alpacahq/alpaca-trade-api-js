import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

/**
 * Single auto-generated sidebar: it picks up curated guides, the migration page
 * derived from root MIGRATION.md, and the generated `api/` reference section,
 * so regenerated pages appear without hand-editing this file.
 */
const sidebars: SidebarsConfig = {
    docsSidebar: [{ type: "autogenerated", dirName: "." }],
};

export default sidebars;
