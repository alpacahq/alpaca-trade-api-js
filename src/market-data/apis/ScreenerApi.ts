/* tslint:disable */
/* eslint-disable */
/**
 * Market Data API
 * Access real-time and historical market data for US equities, options, crypto, and foreign exchange data through the Alpaca REST and WebSocket APIs. There are APIs for Stock Pricing, Option Pricing, Crypto Pricing, Forex, Logos, Fixed income, Corporate Actions, Screener, and News. 
 *
 * The version of the OpenAPI document: 1.1
 * Contact: support@alpaca.markets
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  MarketType,
  MostActivesResp,
  MoversResp,
} from '../models/index';
import {
    MarketTypeFromJSON,
    MarketTypeToJSON,
    MostActivesRespFromJSON,
    MostActivesRespToJSON,
    MoversRespFromJSON,
    MoversRespToJSON,
} from '../models/index';

export interface MostActivesRequest {
    by?: MostActivesByEnum;
    top?: number;
}

export interface MoversRequest {
    marketType: MarketType;
    top?: number;
}

/**
 * 
 */
export class ScreenerApi extends runtime.BaseAPI {

    /**
     * Returns the most active stocks by volume or trade count based on real time SIP data. By default, returns the top 10 symbols by volume. 
     * Most active stocks
     */
    async mostActivesRaw(requestParameters: MostActivesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MostActivesResp>> {
        const queryParameters: any = {};

        if (requestParameters['by'] != null) {
            queryParameters['by'] = requestParameters['by'];
        }

        if (requestParameters['top'] != null) {
            queryParameters['top'] = requestParameters['top'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["APCA-API-KEY-ID"] = await this.configuration.apiKey("APCA-API-KEY-ID"); // apiKey authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["APCA-API-SECRET-KEY"] = await this.configuration.apiKey("APCA-API-SECRET-KEY"); // apiSecret authentication
        }


        let urlPath = `/v1beta1/screener/stocks/most-actives`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MostActivesRespFromJSON(jsonValue));
    }

    /**
     * Returns the most active stocks by volume or trade count based on real time SIP data. By default, returns the top 10 symbols by volume. 
     * Most active stocks
     */
    async mostActives(requestParameters: MostActivesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MostActivesResp> {
        const response = await this.mostActivesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the top market movers (gainers and losers) based on real time SIP data. The change for each symbol is calculated from the previous closing price and the latest closing price.  For stocks, the endpoint resets at market open. Until then, it shows the previous market day\'s movers. The data is split-adjusted. Only tradable symbols in exchanges are included.  For crypto, the endpoint resets at midnight.
     * Top market movers
     */
    async moversRaw(requestParameters: MoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MoversResp>> {
        if (requestParameters['marketType'] == null) {
            throw new runtime.RequiredError(
                'marketType',
                'Required parameter "marketType" was null or undefined when calling movers().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['top'] != null) {
            queryParameters['top'] = requestParameters['top'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["APCA-API-KEY-ID"] = await this.configuration.apiKey("APCA-API-KEY-ID"); // apiKey authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["APCA-API-SECRET-KEY"] = await this.configuration.apiKey("APCA-API-SECRET-KEY"); // apiSecret authentication
        }


        let urlPath = `/v1beta1/screener/{market_type}/movers`;
        urlPath = urlPath.replace(`{${"market_type"}}`, encodeURIComponent(String(requestParameters['marketType'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MoversRespFromJSON(jsonValue));
    }

    /**
     * Returns the top market movers (gainers and losers) based on real time SIP data. The change for each symbol is calculated from the previous closing price and the latest closing price.  For stocks, the endpoint resets at market open. Until then, it shows the previous market day\'s movers. The data is split-adjusted. Only tradable symbols in exchanges are included.  For crypto, the endpoint resets at midnight.
     * Top market movers
     */
    async movers(requestParameters: MoversRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MoversResp> {
        const response = await this.moversRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const MostActivesByEnum = {
    Volume: 'volume',
    Trades: 'trades'
} as const;
export type MostActivesByEnum = typeof MostActivesByEnum[keyof typeof MostActivesByEnum];
